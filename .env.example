# Keep this file as a template; put real values in .env (gitignored).
HOST=0.0.0.0
PORT=8787
# Generate with: openssl rand -hex 24
PET_TOKEN=replace-with-a-long-random-pairing-token
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Desktop Codey activity companion (no OpenAI API key required):
CODEX_PET=0
